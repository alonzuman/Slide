import { StyleSheet } from "react-native";

export default StyleSheet.create({
  textShadow: {
    textShadowColor: "rgba(0, 0, 0, 0.2)",
    textShadowOffset: {
      width: 0,
      height: 0,
    },
    textShadowRadius: 4,
  },
});
