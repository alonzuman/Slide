import React from 'react'
import { View, Text } from 'react-native'
import Typography from '../../core/Typography'

export default function StreamFooter() {
  return (
    <View style={{ backgroundColor: 'green' }}>
      <Typography>Stream Footer</Typography>
    </View>
  )
}
