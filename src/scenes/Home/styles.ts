import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerRightButton: {
    marginRight: 12,
  },

  header: {
    flexDirection: "row",
    alignItems: "center",
  },
});
